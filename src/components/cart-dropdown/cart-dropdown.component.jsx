import React from 'react';
import './cart-dropdown.styles.scss';

const CartDropdown = () => {
    return  (
        <div className='cart-dropdown'>
            <button className='cart-dropdown-btn' type='submit'>go to checkout</button>
        </div>
    );
};

export default CartDropdown;